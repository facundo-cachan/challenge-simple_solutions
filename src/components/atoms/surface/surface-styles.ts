import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
  container: {
    borderRadius: 10,
    flexWrap: 'wrap',
    padding: 10,
    width: "auto",
  }
})

export default styles
